import { expect, test } from "bun:test";

// Phase 0 placeholder so the frontend participates in the test pipeline.
// Component/state-machine tests arrive with those features (see AGENTS.md).
test("frontend workspace wired into the test pipeline", () => {
  expect(true).toBe(true);
});
